# KingAI Markdown Converter v1.1 Requirements
# Install: pip install -r requirements.txt

# Core conversion library (Microsoft)
markitdown[all]>=0.1.0

# PyMuPDF for LLM/RAG optimized extraction
pymupdf>=1.24.0
pymupdf4llm>=0.0.10

# PDF handling
pdfplumber>=0.10.0
pdfminer.six>=20221105

# Progress bar (optional but recommended)
tqdm>=4.65.0

# Office documents
mammoth>=1.6.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.21

# Other formats
beautifulsoup4>=4.12.0
lxml>=4.9.0
xlrd>=2.0.0
pandas>=2.0.0

# Encoding detection
charset-normalizer>=3.0.0
magika>=0.5.0

# v1.1: Memory monitoring for adaptive worker scaling (optional)
psutil>=5.9.0

# v1.1: Rich terminal output (optional, falls back to tqdm/print)
rich>=13.0.0

# Optional: Image OCR support (uncomment if needed)
# Pillow>=10.0.0
# pytesseract>=0.3.10
